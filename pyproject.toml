[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "electricity-demand-forecasting"
version = "0.1.0"
description = "Initial scaffold for an electricity demand forecasting MLOps project."
readme = "README.md"
requires-python = ">=3.11"
